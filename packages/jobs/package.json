{
  "name": "@showzy/jobs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Server-only job runner port over pg-boss (ADR-0041).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "pg-boss": "12.31.0"
  }
}
