/**
 * Locale detection and template interpolation shared by every copy
 * namespace. Implementation lives in `@showzy/copy/locale`.
 */
export { detectLocale, interpolate, type Locale } from "@showzy/copy/locale";
