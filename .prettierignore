# Generated files
pnpm-lock.yaml

# drizzle-kit output must stay byte-identical to what the generator emits,
# or the migration drift check (fnd-T5) would diff formatting, not schema.
packages/db/migrations/

# OpenAPI artifact must stay byte-identical to generation, or the
# OpenAPI drift check (fnd-T25) would diff formatting, not the contract.
packages/contract/openapi.json

# Human/pipeline-authored documents. docs/specs/* are frozen contracts that
# implementers may not edit (constitution.md), so Prettier never rewrites
# anything under docs/ or the agent instruction files. The Claude Code harness
# (rules, skills incl. vendored ones, agents, scripts, worktrees) is not
# Prettier's either (ADR-0040).
docs/
.claude/

# TanStack Router codegen must stay byte-identical to what the generator
# emits (SHO-309); the router plugin rewrites it on every dev/build run.
apps/web/src/routeTree.gen.ts

# Playwright HTML/trace output (SHO-331).
apps/web/playwright-report/
apps/web/test-results/

# Vendored UAPKI / WASM / Nitro codegen — not Prettier's (SHO-252).
packages/document-signing/cpp/
packages/document-signing/wasm/
packages/document-signing/nitrogen/
packages/document-signing/android/
packages/document-signing/ios/
packages/document-signing/cpp-bridge/
